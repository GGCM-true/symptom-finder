# 任务总览：省医 · 症状导诊查询工具

## 交付成果
在 `shengyi-triage/` 目录下完成一套「症状 → 挂号科室」的**响应式网页查询工具**（以四川省人民医院本部为例），并扩展为**静态页 + MySQL/Oracle 双数据库 + Spring Boot API** 多形态版本：

| 文件 | 说明 |
|---|---|
| `index.html` | 查询界面：症状搜索 + 结果卡片 + 科室浏览 + **组合分诊 + 人体图点选（13 部位）** |
| `guahao.html` | 模拟挂号页（科室/楼层/诊室/费用/医生），支持 `?dept=` 与 `#dept=` 路由 |
| `data.js` | 结构化数据层：**135 条**症状条目 + 31 科室 + 科室挂接映射（单一数据源） |
| `sql/` | MySQL 全量建库脚本 + **`append_2026-09-09.sql` 增量追加脚本** |
| `sql/oracle/` | Oracle 23ai 全套脚本（建用户/建表/数据/练习） |
| `backend/` | Spring Boot 3 + MyBatis REST API（本地编译打包成功） |
| `tools/` | 导出脚本 + `smoke_test.js` 页面冒烟测试 |
| `README.md` / `data-entry-guide.md` / `git-cheatsheet.md` | 说明文档 |

## 2026-09-09 人体图扩充
用户反馈「按身体部位找症状对应的疾病太少」，本次完成：

1. **量化盘点**：原 105 条中仅 73 条挂入人体图部位，**32 条未挂载**（中风/晕厥/血尿/儿童/妇科/泌尿/精神心理等）。
2. **32 条全部归位**：脑梗/晕厥/头部外伤→头面；咳嗽/喘息→胸；妇科/泌尿/儿童腹症→腹盆腔；血尿/蛋白尿→腰背肾区；外伤→上肢+下肢；儿童皮疹→皮肤；慢病/急症/情绪→全身性 等。
3. **新增 30 条**高发症状（135 条总计）：干眼/麦粒肿/结膜炎/斜视、中耳炎/外耳道炎/耵聍/耳鸣/晕车、过敏性鼻炎/鼻窦炎/嗅觉减退/腺样体、口臭/智齿/颞下颌关节/牙齿敏感、扁桃体炎/落枕、闪腰/脊柱侧弯、网球肘/鼠标手/扳机指/腱鞘炎、膝痛/崴脚/腿抽筋/髋痛 等。
4. **刻意去重 4 条**（数据保留可搜索，不重复挂部位）：mental、sore-throat、hemorrhoid、amenorrhea，分别与已挂的 anxiety+depression、throat、hematochezia、menstrual 语义重叠。
5. **部位覆盖从 3~13 条 → 7~21 条**（仅腹部因含消化+妇科+泌尿达 21 条，其余 7~15 条）。
6. **SQL 同步**：全量脚本刷新至 135 条；新增**幂等增量脚本** `sql/append_2026-09-09.sql`（INSERT IGNORE，105→135 平滑升级，不 DROP 库）。
7. **冒烟测试**：`tools/smoke_test.js` 通过——index.html / guahao.html 均无异常，0 id 重复、0 科室解析失败。

## 关键决策
1. 技术路线：静态网页 + JSON 数据分离，`data.js` 同时充当数据库；后续可平滑迁移 MySQL/Oracle/后端 API。
2. 科室口径以省医院官网（samsph.cn）公示为准（2026-09 核对），含独立发热门诊、眩晕/头痛专病门诊等真实信息。
3. 每条含三档分诊级别（🟢 门诊 / 🟡 尽快 / 🔴 急诊）+ 急诊红线 + 医疗免责声明，定位科普参考。
4. 人体图扩充采用「先归位、再补缺」策略：未挂载词条按部位归位优先级最高（零成本），之后按部位缺口补齐高发常见症状。

## 后续可做
- 已有 105 条旧版数据库的升级：执行 `sql/append_2026-09-09.sql`（幂等，可重复执行）。
- 把扩充同步推送到 GitHub / 线上分享链接（当前线上为旧版）。
- 将人体图扩充逻辑同步到后端 API（数据库版已含全部 135 条）。
- 对外使用前建议请院内医生复核内容。
